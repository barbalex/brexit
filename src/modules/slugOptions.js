//      
// if mark is not true, "!" in text are not kept
// so if you change title to include it, doc name stays same
// this creates conflict and doc is lost!
export default {
  lang: 'de',
  symbols: true,
  uric: false,
}
